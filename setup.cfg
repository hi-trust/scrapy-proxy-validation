
# See the docstring in versioneer.py for instructions. Note that you must
# re-run 'versioneer.py setup' after changing this section, and commit the
# resulting files.

[versioneer]
VCS = git
style = pep440
versionfile_source = scrapy_proxy_validation/_version.py
versionfile_build = scrapy_proxy_validation/_version.py
tag_prefix =
parentdir_prefix =

